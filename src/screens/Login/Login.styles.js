import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    card: {
        position: 'absolute',
        top: 110,
        width:'90%',
        //height:100

    },
    inputsView: {
        width: '100%',
        alignItems: 'center'
    }
})
export default styles