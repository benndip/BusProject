import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '85%',
        height: 55,
        marginVertical: 10,
        flexDirection: 'row',
        alignItems: 'center',
        paddingHorizontal: 10,
        backgroundColor: 'rgba(69, 170, 242,.2)',
        borderRadius: 5

    },
    input: {
        width: '100%',
        height: '100%',
        borderWidth: 1,
        paddingHorizontal: 10,
    }
});

export default styles