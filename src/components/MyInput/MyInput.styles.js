import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '85%',
        height: 30,
        borderColor: 'grey',
        borderWidth: 1,
        marginVertical: 10,
    }
});

export default styles