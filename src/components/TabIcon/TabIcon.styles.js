import theme from '../../theme';

let styles = {
    text: {
        color: theme.BLACK_COLOR, 
    	fontSize: theme.FONT_SIZE_NORMAL,
    	fontFamily: theme.FONG_FAMILY,
    }, 
    focusedColor: theme.PRIMARY_COLOR, 

    mainView:{
         width:'70%', 
         height:32,
         justifyContent: 'center', 
         alignItems: 'center',
         //backgroundColor:'red'
        }
}

export default styles;